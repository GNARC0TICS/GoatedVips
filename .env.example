# Goombas x Goated VIPs v2.0 Environment Configuration

# Application Settings
NODE_ENV=development
PORT=3000

# Database Configuration
# For local development, use a local PostgreSQL instance
# For production, use a cloud provider like Neon, Supabase, or AWS RDS
DATABASE_URL=postgresql://username:password@localhost:5432/goombas_x_goated_vips

# Redis Configuration (Optional - will use memory store if not provided)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Authentication Secrets
# IMPORTANT: Change these in production to strong, unique values
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-make-it-long-and-random
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-change-in-production-make-it-different

# CORS Configuration
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:5174,http://localhost:3000

# Email Configuration (Optional - for user verification and password reset)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=combined

# Security Configuration
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=900000
REFRESH_TOKEN_TIMEOUT=604800000

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=10
REGISTER_RATE_LIMIT_MAX=5